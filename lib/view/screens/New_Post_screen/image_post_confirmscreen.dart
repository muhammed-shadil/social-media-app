import 'package:flutter/material.dart';

class ImagePostConfirmscreen extends StatelessWidget {
  const ImagePostConfirmscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(),
      ],
    );
  }
}